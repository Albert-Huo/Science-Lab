# 微信滚动手柄兼容 Implementation Plan

**Goal:** 在微信 X5/WKWebView 中为右侧内容滚动手柄提供可靠的触摸回退。

**Architecture:** 在既有 `bindBand` 内统一 pointer/touch 手势状态。Touch Events 只绑定手柄并优先接管真实触摸，避免改变实验区输入。

## 步骤

- [x] 增加指针捕获失败仍可移动的失败测试。
- [x] 增加纯 Touch Events 滚动和 pointer/touch 去重的失败测试。
- [x] 修改 `experiment-scroll.js`，实现统一手势、非被动触摸监听与确定性清理。
- [x] 更新预览说明和交付记录。
- [x] 运行滚动专项、完整测试、语法与差异检查。
- [x] 使用隔离浏览器模拟微信触摸环境，验证拖动、横滑、轻点和实验编号不变。
- [x] 提供本地 HTTP 预览，并明确真实微信需要公网版本确认。
- [x] 用户追加授权后提交、快进合并到 `main`、推送并原子部署 App 壳。
- [x] 在公网微信移动仿真中复验三个试点；等待用户微信真机最终确认。

## Android 微信真机二次修订（v0.8.3）

- [x] 增加滚动脚本与 Service Worker 版本一致性的失败测试。
- [x] 增加手指移出手柄后由文档级监听继续接管、结束后卸载的失败测试。
- [x] 活动手势期间安装 Pointer/Touch 捕获监听，取消、结束和销毁时确定性清理。
- [x] 增加 `?scroll-debug=1` 本地页面诊断，不记录或上报事件数据。
- [x] 版本化加载并预缓存 `experiment-scroll.js?app=v0.8.3`。
- [x] 运行滚动专项、完整测试、离线依赖审计、语法和差异检查。
- [x] 使用隔离 Android 微信触摸环境，强制指针捕获失败并复验实验 1、35、41。
- [x] 提交、推送并原子部署静态 App 壳；API、nginx 和数据库保持不变。
- [ ] 用户通过带诊断参数的公网入口完成 Android 微信真机确认。

## Android 微信生命周期恢复（v0.8.5）

- [x] 复现“手柄 ready、触摸计数增加、内容不滚动”的接收器失联状态。
- [x] 先增加接收器 `pagehide/pageshow` 恢复失败测试，再实现暂停与恢复。
- [x] 先增加宿主可见性和页面恢复失败测试，再统一生命周期重连。
- [x] 先增加边缘安全失败测试，再将透明触摸带向内移动 18px。
- [x] 先增加 v1→v2 无损迁移失败测试，再升级发布工具和 70 个实验引用。
- [x] 先增加拖动连接确认失败测试，再实现首次拖动自愈。
- [x] 完成 Android 微信移动仿真、桌面回归、70 页边界校验和完整测试。
- [ ] 经用户授权后提交、推送并依次发布内容站与 App 壳，再进行 Android 微信真机验收。
