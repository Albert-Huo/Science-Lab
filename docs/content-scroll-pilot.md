# 当前实验内容滑条：本地试点交付

日期：2026-09-03。分支：`codex/content-scroll-pilot`；基础提交：`170517e6f1e3ca7d6120fc663c7a58b9bce27fed`。

本文件先记录本地试点；用户在体验通过后另行授权提交、推送和部署。正式发布范围仍只包含初中物理实验 1、35、41，发布状态见末尾记录。

## 试用

- 电脑友好入口：http://127.0.0.1:18890/scroll-preview.html
- 原始自适应入口：http://127.0.0.1:18890/?base=/HTML-/
- 试点：初中物理实验 1（温度计）、35（声音特性）、41（视力矫正）。从顶部目录选择。
- 按住右侧半透明 ↕ 短手柄上下拖动。常规视口下可见手柄 14×96px，透明触摸区域 30×112px；上滑查看下面，反向返回。手指移出手柄仍可连续拖动，到头不换实验。
- 矮屏下手柄自动缩短，避开顶部栏和翻页按钮；触摸目标高度不足 44px 时隐藏。
- 旧的左右边缘条及其切换手势已完全删除，不再显示蓝条或留下隐藏拦截区。切换实验使用目录或右下角箭头，“我的/AI”使用顶部按钮。内容不溢出、未适配、面板或完成遮罩打开时隐藏手柄。
- 手柄没有可视进度含义，也不响应轻点跳动、横滑打开目录。键盘与手柄上的滚轮仍可滚动当前内容。
- 899px 以内的窄屏电脑可用鼠标操作同一手柄；宽屏无触摸桌面不显示手柄，带触摸能力的平板及混合设备仍可使用。

仅监听当前电脑，手机访问自身的 127.0.0.1 无法打开。不要直接双击 `index.html`：`file://` 没有可信父页面来源，安全滚动协议会保持禁用。没有开放局域网或公网端口，也没有提供 AI 代理。浏览器移动模拟范围见下面分版本记录；iOS Safari / Android 真机手感仍待用户验收。

重新启动预览（已有服务运行时不要重复启动）：

```sh
cd /Users/lx100/.config/superpowers/worktrees/Science-Lab/content-scroll-pilot
node tools/preview-scroll.mjs --content-root /Users/lx100/projects/HTML-GitHub/HTML- --port 18890
```

## 2026-09-04 体验反馈修订

- 根因复现：390×844 窄屏在 `pointer: coarse` 为 false 时，旧右侧 `.rail` 仍显示，新手柄 CSS 和通信初始化却被关闭。
- 修订后旧 `edgeL`、`edgeR`、`.rail` 与 `attachEdge` 均不存在；独立 `scrollHandle` 只负责当前实验内容滚动。
- 启用条件改为“视口不宽于 899px，或设备存在粗指针”，不再依赖单一主指针判断。
- 可视手柄保持 14px 宽，改用中性深灰半透明底和浅色边框；按下状态不使用蓝色。
- 滚动专项 14/14、预览专项 1/1、完整 `npm test --prefix server/api` 均通过，脚本语法与差异空白检查通过。
- 390×844 精细指针实测：实验 1、35、41 分别滚动 60、72、72px，编号保持不变；实验 42 无手柄，右缘拖动也不换实验。
- 390×844 触摸上下文确认粗指针识别、30×112px 触摸区、14×96px 可视手柄及无旧条；1280×900 无触摸桌面隐藏手柄，右下角按钮仍能切换实验。
- 打开“我的”面板时手柄隐藏，关闭面板后恢复，避免与覆盖层争夺触摸。
- 浏览器控制台 0 错误、0 警告。截图保存于：
  `/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-04-scroll-handle-cleanup/output/playwright/scroll-handle-cleanup-mobile.png`。
- 用户在本地预览确认后授权提交、推送并部署；发布时将 Service Worker 缓存版本升级为 `v0.8.1`，防止已安装客户端继续使用旧首页。
- App 功能提交 `659d71c79d8963a48c6515b199831b449f189243` 已推送；阿里云静态站已原子切换到 `/var/www/science-lab-releases/20260904-659d71c79d89`，上一版 `/var/www/science-lab-releases/20260904-43ced6ff36ca` 保留为回滚目标。API 进程、API release、nginx 与 systemd 配置未改动。
- 正式公网 390×844 触摸上下文复验实验 1、35、41：均从 0 滚动到 60px 且编号不变；实验 42 无手柄，右缘拖动不换实验；源码和 DOM 均无旧边缘条。
- 正式公网 Service Worker `v0.8.1` 成功接管，缓存中包含 `experiment-scroll.js`，离线重载 App 壳通过。

## 2026-09-04 微信内置浏览器兼容修订（待真机确认）

- 现象：普通浏览器可拖动，微信内可看到手柄但拖动不生效。手柄可见说明页面版本、宿主与实验 iframe 通信、内容溢出判断都已生效；问题集中在输入事件兼容层。
- `Pointer Events` 仍是主路径；当微信内核不支持或拒绝 `setPointerCapture` 时，不再中止整个手势，而是在手指仍位于手柄区域时继续处理。
- 手柄新增非被动 `Touch Events` 回退，仅作用于 30×112px 触摸区，不修改实验区触摸行为；Pointer 与 Touch 同时产生时由 Touch 接管并清理前一状态，避免同一次拖动滚动两遍。
- 维持原有 8px 方向锁：纵向拖动滚动内容，横向拖动、轻点和多指手势都不会切换实验；结束、取消及销毁都会清理活动状态和已取得的指针捕获。
- 本地预览一度出现 100px 拖动滚动 200px，事件追踪确认不是输入层重复，而是正式内容已有接收器后预览工具又注入一份。预览注入现已改为幂等，并新增“已有接收器只保留一份”的回归测试。
- 隔离的 390×844 Chromium 使用微信 Android User-Agent、触摸上下文并强制 `setPointerCapture` 抛错：实验 1 从 0 滚到 100px，实验 35、41 均从 0 滚到 60px，三个实验编号分别保持 1/118、36/118、42/118；实验 1 横拖与轻点后仍为 0。控制台 0 错误，只有预期的兼容模式警告。
- 验证截图：`/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-04-wechat-scroll-fallback/output/playwright/.playwright-cli/page-2026-09-04T03-10-14-592Z.png`。
- 自动化检查只能模拟微信输入组合。正式推送和部署前，仍需在真实微信内确认实验 1、35、41 可上下拖动且实验编号不变。
- 用户随后授权发布：设计提交 `b18ee6c8f055445efb90141bc19f2c64f4ddfb04` 与功能提交 `ae9f5756abb9126fe0600915462735b973d4eaab` 已快进合并并推送到 `main`。
- Service Worker 升级为 `v0.8.2`；阿里云静态站已原子切换到 `/var/www/science-lab-releases/20260904-ae9f5756abb9`，上一版 `/var/www/science-lab-releases/20260904-659d71c79d89` 保留为回滚目标。API、nginx 和数据库未改动，API 内外网健康检查均通过。
- 公网 390×844 微信 Android 仿真复验实际返回 Touch 回退代码和 `v0.8.2`：强制指针捕获失败后，实验 1、35、41 均从 0 滚动到 60px，编号保持 1/118、36/118、42/118；控制台 0 错误。最终结论仍以用户的微信真机体验为准。
- 公网验证截图：`/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-04-wechat-scroll-fallback/output/playwright/.playwright-cli/page-2026-09-04T03-22-10-839Z.png`。

## 2026-09-04 Android 微信二次修订（v0.8.3，待真机确认）

- 真机反馈仍无法拖动后，补充确认线上 HTML 虽已更新，但滚动脚本使用未版本化 URL；旧 Service Worker 可能先返回 `v0.8.1` 缓存。`index.html` 与预缓存现统一使用 `experiment-scroll.js?app=v0.8.3`，带查询参数的验收入口也会绕开旧首页精确缓存。
- Pointer/Touch 的移动、结束和取消监听改为仅在活动手势期间安装到宿主文档捕获阶段；手指移出 30×112px 短手柄后仍可继续接收同一手势，结束后立即卸载，避免常驻非被动监听影响普通页面滚动。
- 增加可选的 `?scroll-debug=1` 页面诊断条，显示脚本版本、手柄可用状态、当前滚动位置及 Pointer/Touch 计数。诊断不写存储、不发送网络请求，普通入口不显示。
- 新增版本一致性、页面级事件接管和监听清理测试。滚动专项 18/18、完整 `npm test --prefix server/api`、离线依赖审计、脚本语法和差异空白检查均通过。
- 隔离的 390×844 Chromium 使用 Android 微信 User-Agent 和触摸上下文，并强制 `setPointerCapture` 失败：实验 1 手指从手柄内拖到手柄外后滚动 140px；实验 35、41 各滚动 80px，编号保持 1/118、36/118、42/118。控制台 0 错误，只有预期兼容警告。
- 验证截图：`/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-04-android-scroll-v083/output/playwright/.playwright-cli/page-2026-09-04T04-10-09-916Z.png`。自动化不能等同于真实 X5，最终结论仍以 Android 微信真机为准。

## 单手柄改版验证记录

- 滚动测试 13/13、预览服务测试 1/1 通过；新增行为及矮屏规则均先观察失败再补实现。
- 本次改版全量 `npm test --prefix server/api` 退出 0。其后补矮屏 CSS 和可用高度判断，再次运行上述 14 项相关测试及脚本语法、差异空白检查，通过。
- 实际实验 1 的 390×844 截图已目检：左侧白条和右侧绿色进度条消失，右侧只保留 14×96px 中性短手柄；26×112px 触摸区域之外不覆盖实验。
- 实际实验 1 已通过拖出手柄滚到内容底部、不换实验、手柄位置和尺寸不随进度变化的浏览器断言，保留截图。
- 隔离轻量内容测试页配合真实宿主及接收器：拖出短手柄滚动 250px、横滑和轻点无动作、滚轮与键盘仅滚内容、遮罩隐藏手柄、左侧触摸穿透均通过。390×844 时触摸区域为 26×112px；667×260 时为 26×66px，底部 y=126 小于导航顶部 y=146；高度 220px 时手柄隐藏。该项不是三个真实实验的完整回归。
- 1280×900 隔离桌面测试页：非粗指针模式、左侧原区域显示、移动手柄隐藏、右侧原拖动切换实验通过。
- 最初一次新版 1/35/41 浏览器回归曾因机器高负载中断；正式发布准备阶段已改用待发布的真实 App 与内容文件，补跑并完成全部三页回归。
- 独立只读复核无关键/重要问题；发现的矮屏重叠已修正并二次复核。260px 高且无安全区时，手柄区域为 y=60–126，与翻页按钮顶部 y=146 保持 20px 间距。

新版截图及复核脚本：
`/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-03-single-scroll-handle/output/playwright/`。

## 第一版验证记录（改版前，保留追溯）

以下是原双色进度条版本的验证记录；其中左侧翻页、横滑目录已被本次用户批准的“单条单用途”设计移除，不能作为新版功能说明。

- 新增滚动测试 9/9、预览服务测试 1/1，通过。先观察缺失实现／缺失遮罩适配导致失败，再补实现。
- 全量 `npm test --prefix server/api` 退出 0；脚本语法、HTML 内联脚本解析、`git diff --check` 通过。
- 390×844 触摸模拟：三个试点到顶/底反复滑动不换实验，取消捕获、目录/我的面板、真实完成遮罩节点、切换、刷新、横竖屏检查通过。
- 三个试点的实际溢出高度分别为 258、268、150 CSS px。不是通过改变实验高度制造滚动。
- 器材触摸：温度计横向位置从 161 移动到 111.3125；钢尺从 16 cm 变为 8 cm。模式按钮仍响应。
- 双域名验证：隔离浏览器分别使用 `https://lab.xingnian.net.cn` 与 `https://html.xingnian.net.cn` 的真实不同 origin，请求内容由本地响应提供；未访问或修改正式站。滑条成功滚动 258px，横滑目录通过。
- 无溢出测试页隐藏滑条；未适配实验 42 不显示滑条且右侧不会误翻页；左侧手势仍翻页。
- 1280×900 桌面原有右侧翻页不变；电脑手机尺寸预览中鼠标拖动通过。
- 独立复核发现实际 `.modal-mask` 遮罩未匹配；已同时补检测和属性观察，加入回归测试，三个试点浏览器复验通过。复核无剩余本地试点阻塞项。

测试只涉及滚动接入及相关回归，不是三个实验的完整物理教学/全部关卡审计。

## 文件改动清单

- `index.html`：右侧滑条样式、状态显示、当前 iframe 生命周期、指针类型分流、操作说明。
- `experiment-scroll.js`：宿主通信与手势处理。
- `experiment-scroll-receiver.js`：实验端滚动适配，验证来源、窗口、会话及数值；不操作器材状态。
- `tools/preview-scroll.mjs`、`scroll-preview.html`：本机预览、3 页响应注入、桌面试用入口。
- `server/api/test/frontend-scroll.js`、`server/api/test/preview-scroll.js`、`server/api/package.json`：测试与测试入口；没有修改后端服务逻辑。
- 本文件及 `docs/superpowers/specs/2026-09-03-content-scroll-pilot.md`、`docs/superpowers/plans/2026-09-03-content-scroll-pilot.md`、`docs/superpowers/plans/2026-09-03-single-scroll-handle.md`：设计、执行与交付记录。

第一版截图与浏览器检查代码保存在私有仓库（旧的左侧及横滑断言不适用于新版）：
`/Users/lx100/projects/HTML-GitHub/HTML-sources-private/reports/visual-regression/2026-09-03-content-scroll-pilot/output/playwright/`。

## 正式发布前的解除条件

1. 用户确认细条显眼程度、26px 触摸带是否易操作，以及手机真机手感；首版无惯性续滑。
   新版完整的 1/35/41 真实实验回归须补跑；公开内容仓库同时存在其他任务的未提交修改，本任务未写入或回退这些文件，发布前须锁定待发布版本再验证。
2. 为内容站正式接入接收器。先发布兼容接收器，再发布宿主；未适配的实验继续隐藏滑条。
3. 不自动猜测复杂嵌套滚动区域。当前三个试点使用文档滚动根；内部容器需逐页明确指定并验证。
4. 将新宿主脚本纳入实际静态部署清单和 Service Worker 缓存，更新版本，测试离线/旧缓存升级；本轮尚未做这些发布改动。
5. 逐批扩大覆盖，并验证该批实验的遮罩、抽屉、器材与移动滚动。不要把本地注入效果理解为全站已经适配。

## 正式发布准备记录（2026-09-04）

- 内容站从远端 `88231ce8c7006a65ac6ed58106aef85be5319062` 建立隔离工作树，仅三份试点 HTML 各内联一次与 `experiment-scroll-receiver.js` 逐字一致的接收器；校验确认移除该块后与基线逐字一致，没有夹带原工作目录的并行修改。
- App `sw.js` 升级为 v0.8.0 并预缓存 `experiment-scroll.js`；阿里云静态清单从十个物理文件更新为十一个。接收器源文件和本机预览文件不进入 ECS 静态 release。
- 390×844 待发布真实文件回归：实验 1、35、41 的溢出分别为 258、268、150px；到顶/底、拖出捕获、横滑/轻点无动作、滚轮/键盘、遮罩/面板、未适配页、温度计拖动、刷新和横屏均通过。
- 双真实 Origin 隔离验证通过：逻辑地址分别为 `https://lab.xingnian.net.cn` 与 `https://html.xingnian.net.cn`，响应来自本地候选文件，不访问或修改正式站；内容成功滚动且不换实验。
- Service Worker v0.8.0 安装、滚动宿主脚本入缓存、离线重载 App 壳通过。实验内容仍不由 App 缓存。
- 浏览器发现并修正 iframe 未加载完成时的过早 `postMessage`：现在仅在 load 后握手，对应回归测试先失败再通过，复验不再出现该告警。
